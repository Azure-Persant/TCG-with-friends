{
  "name": "@fci/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Builds a throwaway local/CI database from the schema files.",
  "scripts": {
    "apply:local": "node apply.mjs --local",
    "test": "node apply.mjs --local --tests"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
