{
  "name": "@fci/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Applies the database files, in order, to any Postgres.",
  "scripts": {
    "apply": "node apply.mjs",
    "apply:local": "node apply.mjs --local",
    "test": "node apply.mjs --local --tests"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
